# 造化笔插件开发指南

造化笔（zaohuabi）的插件是**一个文件夹**：一个 `manifest.json` 声明身份与权限，一个 `index.js` 在受限沙箱里注册功能。
不需要 npm、不需要编译、不需要打包——把文件夹放进插件目录、点一下「重新扫描」就能用。

- 官方插件（随安装包发布）：仓库根 `plugins-official/`，启动时自动装进用户插件目录。
- 开发示例（四个，可直接照抄）：仓库根 `plugins-samples/`，插件页点「安装示例」即装。
- 插件系统实现：`src/main/pluginManager.ts`（注册与沙箱）、`src/shared/types.ts:686`（权限表）、`src/shared/types.ts:702`（槽位表）、`src/renderer/src/plugins/sanitize.ts`（HTML 清洗）、`src/renderer/src/plugins/PluginSlots.tsx`（渲染端槽位与浮动角色）。

---

## 一、5 分钟上手

### 1. 最小插件

新建文件夹（名字随意，比如 `我的插件`），里面放两个文件：

```
我的插件/
├── manifest.json
└── index.js
```

`manifest.json`

```json
{
  "name": "我的插件",
  "version": "1.0.0",
  "description": "在项目页头加一个按钮，显示全书字数。",
  "author": "你的名字",
  "permissions": ["data:read", "ui"],
  "entry": "index.js"
}
```

`index.js`

```js
module.exports = {
  onLoad(api) {
    api.ui.addToolbarButton({ id: 'my-count', slot: 'project.header', title: '全书字数', icon: '📊' }, (ctx) => {
      const st = api.data.stats(ctx.bookId)
      return '全书 ' + st.total + ' 字 · 今日 ' + st.today + ' 字'
    })
    console.log('我的插件已挂载')
  }
}
```

### 2. 装进造化笔

1. 打开造化笔 → 侧栏「插件」页。
2. 点右上角**「打开插件目录」**（会打开系统文件管理器，路径是 `%APPDATA%\造化笔\plugins`；macOS / Linux 是各自的 userData 目录）。
3. 把上面那个文件夹整个拷进去。
4. 回到插件页点**「重新扫描」**。
5. 卡片出现后，把右侧开关打开（**启用中**）；`data:read` / `ui` 这类低风险权限默认放行，高风险权限要单独点芯片授予（见第五节）。

之后改代码，只要再点一次「重新扫描」即可重新加载（插件没有热更新）。

### 3. 看日志

插件的 `console.log` 会带前缀出现在造化笔的日志里：

```
[plugin:我的插件] 我的插件已挂载
```

加载失败时，插件卡上会出现红色**「加载错误」**与错误文本（这也是最快的排错入口）。开发时建议开 DevTools 看渲染端日志，主进程日志看启动它的终端。

---

## 二、插件长什么样

### 目录结构

```
<插件目录>/<任意文件夹名>/
├── manifest.json     ← 必需
├── index.js          ← 入口，默认名 index.js，可用 manifest.entry 改
├── 不高兴.png         ← 插件自己的资源（图片、数据…），只能放在这个目录内
└── data.json         ← 需要静态数据时用 require('./data.json') 读
```

- 插件目录：`<userData>/plugins`，**只扫一层**（直接子文件夹，`.开头`的目录被跳过）。
- **文件夹名 ≠ 插件名**：插件名取自 `manifest.json` 的 `name`，启用状态与权限授予都按 `name` 记。
- 同一个插件目录里，`name` 重复只留一个。

### manifest.json 字段

| 字段 | 必填 | 说明 |
|---|---|---|
| `name` | ✅ | 插件名，界面上显示的名字；**缺失或为空则该插件不被识别**。 |
| `version` | ✅ | 版本号字符串，如 `"1.0.0"`；**缺失则不被识别**。官方插件靠它决定要不要整目录替换（见第十节）。 |
| `description` | ⭕ | 插件卡上的一行介绍。**请用一句话概括主要功能**，不要把更新日志写进来（用户明确要求过）。 |
| `author` | ⭕ | 作者，显示在插件卡上。 |
| `permissions` | ⭕ | 权限数组，默认 `[]`（等于什么都不许做）。见第五节。 |
| `entry` | ⭕ | 入口文件名，默认 `index.js`；只能是插件目录内的文件名。 |

### 入口

两种写法都可以（`src/main/pluginManager.ts:261-271`）：

```js
// 写法 A：导出对象带 onLoad（推荐）
module.exports = { onLoad(api) { /* ... */ } }

// 写法 B：直接导出函数
module.exports = function (api) { /* ... */ }
```

- `onLoad(api)` 在插件**每次加载时**被调用一次：启用插件、点「重新扫描」、改权限都会重新加载。
- **没有 onUnload**：停用插件时宿主会自动清掉你用 `api.hooks.on(...)` 注册的监听；你自己开的定时器请在改用 `setTimeout` + 判断状态的方式收敛，或干脆只用 `api.hooks`。
- 注册回调里抛的异常会被宿主兜住并记日志，通常只是那一次动作失败，不会拖垮软件（面板与统计规则各自有兜错）。

---

## 三、沙箱：有什么、没有什么

插件的 `index.js` 跑在 `vm` 沙箱里（`src/main/pluginManager.ts:280-307`），**不是完整的 Node 环境**。

**有的**：`console`（自动带 `[plugin:名字]` 前缀）、`module` / `exports`、`require`（仅限自己目录内的 `.json`）、`setTimeout` / `clearTimeout` / `setInterval` / `clearInterval`、`Date`、`Math`、`JSON`、`Object`、`Array`、`String`、`Number`、`Boolean`、`RegExp`、`Error`、`Promise`、`URL`、`TextEncoder`、`TextDecoder`。

**没有的**：

| 想做 | 结果 |
|---|---|
| `require('fs')` / `require('path')` / 任何 Node 内置模块 | 抛错 `插件不能 require 内置模块（受限沙箱）：fs` |
| `require('./tool.js')` 本地 JS | 抛错 `本地 JS require 暂不支持（基础版）`（只支持 `.json`） |
| `require('../../别的地方/x.json')` | 抛错 `插件只能读取自己目录内的文件` |
| `process` / `__dirname` / `global` | 不存在（用不到；插件目录由宿主负责读） |
| `fetch` / `XMLHttpRequest` | 默认不存在；**声明并授予 `network` 权限**后才有 `fetch` |
| 读写书籍数据文件 | 走 `api.data`，不要想绕过 |
| 显示自己的 `<script>` / 内联事件 | 插件返回的 HTML 会被清洗（见第七节） |

**同步回调**：所有回调（工具栏 / 菜单 / 悬浮卡 / 面板 / 统计规则 / 浮动角色）都是**同步**的——宿主拿到返回值就立刻处理，不会 `await` 它。
所以**不要写 `async (ctx) => {...}`**，返回 Promise 的结果是字符串 `"[object Promise]"`。需要耗时操作时用「先在回调里记状态、再用 setTimeout 推进」的写法，或只读现成的 `api.data`（本地数据都是同步返回的）。

---

## 四、API 手册

`onLoad(api)` 拿到的 `api` 上有：

- `api.name`：插件名（字符串）。
- `api.permissions`：**当前实际生效**的权限数组（声明了但没授予的高风险权限不在这里面）。
- `api.data` / `api.ui` / `api.export` / `api.stats` / `api.theme` / `api.ai` / `api.hooks`：见下。

### 4.1 `api.data`——读书与写书

**读（权限 `data:read`，全部同步返回）**

| 方法 | 返回 |
|---|---|
| `listBooks()` | `BookSummary[]`：`{ meta, nodeCount, trashed, folder?, worldId? }`。**书名和书 id 在 `meta` 上**：`b.meta.id` / `b.meta.name` |
| `readBook(bookId)` | `{ meta, structure }` 整本书 |
| `structure(bookId)` | `BookStructure`：`{ nodes, rootId, ... }`，`nodes` 是以节点 id 为键的对象，节点含 `id / kind('group'\|'content') / title / parentId / order / wordCount / roleIds / tag` |
| `content(bookId, nodeId)` | 该节点的正文 markdown 字符串 |
| `roles(bookId)` | `RoleCard[]` |
| `role(bookId, roleId)` | `RoleCard \| null` |
| `roleExperiences(bookId, roleId)` | `RoleExperienceItem[]`（登场记录，项含 `sceneTitle` 等） |
| `world(bookId)` | `WorldEntry[]`（设定库） |
| `timeline(bookId)` | `TimelineEvent[]` |
| `worlds()` | `WorldSummary[]`（世界列表） |
| `worldBooks(worldId)` | `BookSummary[]` |
| `worldOfBook(bookId)` | `{ worldId, name } \| null` |
| `events(worldId)` | `WorldEvent[]` |
| `eventScenes(worldId, eventId)` | `EventSceneRef[]` |
| `search(bookId, query)` | `SearchHit[]` |
| `stats(bookId)` | `BookStats`：`{ total, today, week, streak, calendar, updatedAt }` |
| `snapshots(bookId)` | `SnapshotInfo[]` |
| `compile(bookId)` | 拼好的全书纯文本字符串 |

**写（权限 `data:write`，高风险，需用户在插件页授予）**

```
createBook(input)                        createNode(bookId, input)      updateNode(bookId, nodeId, patch)
removeNode(bookId, nodeId)               saveContent(bookId, nodeId, markdown)
createRole(bookId, input)                updateRole(bookId, roleId, patch)   removeRole(bookId, roleId)
worldCreate(bookId, input)               worldUpdate(bookId, id, patch)      worldRemove(bookId, id)
timelineCreate(bookId, input)            timelineUpdate(bookId, id, patch)   timelineRemove(bookId, id)
eventCreate(worldId, input)              eventUpdate(worldId, id, patch)     eventRemove(worldId, id)
snapshot(bookId, label?)
```

字段含义与内置界面一致，细节见 `造化笔-需求规格书.md` 的数据模型章节；写之前先 `readBook` / `structure` 看清结构。常用的 `BookMeta` 形状：`{ id, name, description, cover, createdAt, updatedAt, wordCount, targetWords, settings, worldId? }`。

### 4.2 `api.ui`——界面扩展（权限 `ui`）

```js
// ① 侧栏面板：先注册，再给打开时的 HTML
api.ui.addPanel({ id: 'overview', title: '本书概况', icon: '📊' })
api.ui.onOpen('overview', ({ bookId }) => '<p>本书概况…</p>')

// ② 工具栏按钮：slot ∈ project.header / canvas.toolbar / editor.toolbar
api.ui.addToolbarButton({ id: 'count', title: '全书概览', icon: '📊', slot: 'project.header' }, (ctx) => '…')

// ③ 右键菜单项：slot ∈ canvas.node / canvas.blank / canvas.link / editor.text
api.ui.addMenuItem({ id: 'menu-words', title: '这个节点有多少字', icon: '🔍', slot: 'canvas.node', danger: false }, (ctx) => '…')

// ④ 节点外观（纯声明式，不回调）：按节点标签 / 类型上色
api.ui.addNodeSkin({ id: 'skin-scene', name: '重点场景', color: '#c79a3a', icon: '★', matchTag: 'scene', matchKind: 'content' })

// ⑤ 悬浮卡：target ∈ node / role，返回的 HTML 追加到对应悬停卡
api.ui.addHoverCard({ id: 'hover-roles', title: '登场角色', target: 'node' }, (ctx) => '<div>…</div>')

// ⑥ 浮动角色：常驻主窗口一角（见第八节）
api.ui.addFloatWidget({ id: 'pet', slot: 'window.bottomRight', title: '我的角色', image: 'pet.png', width: 148 }, (ctx) => ({ html: '…' }))

// 自查可用槽位（写错槽位名会被明确报错）
api.ui.slots()
```

**回调拿到的 ctx**（宿主给的同步快照，字段可能为 `null`，用前判空）：

| 槽位 | ctx 字段 |
|---|---|
| `project.header` | `{ bookId }` |
| `canvas.toolbar` | `{ bookId, layer }`（layer = 当前层节点 id） |
| `editor.toolbar` | `{ bookId, nodeId, title }` |
| `canvas.node` | `{ bookId, nodeId, nodeTitle, nodeKind, nodeTag, linkId, linkKind, layer }` |
| `canvas.link` | 同上（`linkId` / `linkKind` 有值） |
| `canvas.blank` | `{ bookId, nodeId: null, layer }` |
| `editor.text` | `{ bookId, nodeId, title, selection, selStart, selEnd, words }`（`selection` = 选中的原文） |
| 悬浮卡 `node` | `{ bookId, nodeId, title, kind, tag }` |
| 悬浮卡 `role` | `{ bookId, roleId, name }`（注意：`bookId` 此处是世界 id，与角色库接口一致） |
| 浮动角色 | `{}`；宿主点气泡时会再调一次并带 `{ quipOnly: true }`（见第八节） |

**约束**：同一插件内所有界面动作的 `id` 共用一个命名空间，**工具栏 / 菜单 / 悬浮卡 / 浮动角色的 id 不能重名**，否则加载时报 `界面动作 id 重复：xxx`。槽位名写错会直接报 `不支持的槽位：xxx（可用：…）`。

### 4.3 `api.export`——导出格式（权限 `export`）

```js
api.export.addFormat({ id: 'digest', name: '剧情要点', extension: 'md' }, (bookId, { nodeIds }) => {
  // 返回要写进文件的文本；宿主用它弹保存框，文件名 = 插件名-格式名.扩展名
  return '# 剧情要点\n…'
})
```

`extension` 只接受 1–8 位字母数字（`../../etc`、`txt.exe` 会被回落成 `txt`）；导出按钮出现在插件卡上，需要先打开一本书。

### 4.4 `api.stats`——统计规则（权限 `stats`）

```js
api.stats.addRule({ id: 'dialogue', name: '对话行数', desc: '含「」的行数' }, (ctx) => {
  return { text: '128 行', sub: '对话约 3200 字' }   // 也可直接 return 数字或字符串
})
```

返回值只认：`number` / `string` / `{ text, sub? }`；其它类型会在统计页显示成错误。统计页打开时**现算**，所以别在里面做重活——一本书几百个场景逐节点读正文会让统计页变慢。

### 4.5 `api.theme`——主题包（权限 `theme`）

```js
api.theme.addTheme({
  id: 'mint', name: '薄荷', blurb: '清凉薄荷绿', dark: false,
  tokens: {
    '--bg': '#eef7f2', '--bg-soft': '#e3f1e9', '--surface': '#ffffff', '--surface-2': '#f6fbf8',
    '--border': '#cfe6da', '--text': '#1f3329', '--text-dim': '#5f7a6c',
    '--accent': '#2fa07a', '--accent-strong': '#1f7b5c', '--accent-soft': '#d8f0e6'
  }
})
```

- `id` 只能是本地段，最终以 `插件名/本地id` 命名空间化，不会和内置主题冲突。
- **必需的四个令牌**：`--bg`、`--surface`、`--text`、`--accent`（缺任一个报 `主题包缺少必需颜色令牌 --bg`）；其余令牌引擎按明暗自动补全。
- 可用令牌白名单（`src/shared/theme.ts:499-501`）：
  `--bg --bg-soft --surface --surface-2 --border --text --text-dim --accent --accent-strong --accent-soft --danger --danger-soft --warn --warn-soft --shadow --shadow-2 --shadow-3 --deco-1 --deco-2 --deco-3 --bg-grad`。
- `dark: true` 的主题会在暗色模式下正确配对。

### 4.6 `api.ai`——AI 动作与 AI 用量

**注册 AI 动作（权限 `ai`）**：编辑器 AI 面板里多一个动作按钮，指令文本由软件内置的 AI 出口发出。

```js
api.ai.addAction({
  id: 'storyboard', title: '改写成分镜', hint: '把这段改写成 5-8 条分镜', context: 'auto',
  instruction: '把下面这段正文改写成 5-8 条分镜脚本，保留人物与情节顺序，不新增设定。'
})
```

- `instruction` 可以是字符串，也可以是 `(ctx) => string` 按上下文现算。
- `context`：`'auto'`（有选区送选段，默认）/ `'full'`（强制送全文）。
- 插件不接触 API Key，也不直接发请求：请求由主程序代表当前厂商发出，并计入用量台账。

**只读 AI 用量（权限 `ai:read`）**：不含 API Key，是官方插件「造化笔冰冰」的数据来源。

| 方法 | 返回 |
|---|---|
| `api.ai.summary()` | `AiUsageSummary`：余额、今日已用、当前峰谷时段等 |
| `api.ai.recent(n?)` | `AiUsageTurn[]` 最近 n 次调用 |
| `api.ai.days(n?)` | `AiUsageDay[]` 最近 n 天 |
| `api.ai.refresh()` | **触发一次后台余额观测（有节流），立即返回当前台账快照** |
| `api.ai.provider()` | `{ id, name, model, baseUrl, hasKey }` |

注意 `refresh()` 是「后台拉、本次读台账」的同步语义：回调里调用它不会阻塞，也不会立刻拿到联网后的新数字。

### 4.7 `api.hooks`——流程钩子

**订阅事件（权限 `hooks`）**：

```js
const off = api.hooks.on('content:saved', (p) => {
  console.log(p.nodeTitle + ' 保存了 ' + p.wordCount + ' 字')
})
```

| 事件 | 载荷 |
|---|---|
| `content:saved` | `{ bookId, nodeId, nodeTitle, wordCount }` |
| `node:created` | `{ bookId, node: { id, kind, title, parentId, roleIds? } }` |
| `node:updated` | `{ bookId, nodeId, changes, roleIds? }` |
| `node:removed` | `{ bookId, nodeId, deletedIds }` |
| `role:changed` | `{ bookId, nodeId, roleIds }` |

事件是**只通知、不改写**的；改写文本要用下面的内容变换。

**改写正文（权限 `hooks` + `data:write`）**：

```js
// 正文保存时：返回改写后的文本（返回非字符串视为不改写）
api.hooks.onContentTransform((bookId, nodeId, text) => text.split('!!!插').join('★插件改写★'))

// 拼书导出时的文本变换
api.hooks.onTextTransform((text) => text.replace(/--/g, '—'))
```

多个插件/变换按注册顺序链式执行，每个变换都在 `try/catch` 里跑：某一个抛错只跳过它自己。这类钩子会**改动用户的稿子**，务必克制并声明 `data:write`。

---

## 五、权限

权限在 `manifest.json` 里声明，用户在插件页授予（高风险权限**必须手动授予**才生效，低风险默认放行）。

| key | 界面标签 | 高风险 | 管什么 |
|---|---|---|---|
| `data:read` | 读数据 | 否 | 书 / 结构 / 正文 / 角色 / 设定 / 时间线 / 世界 / 世界事件 |
| `data:write` | 写数据 | **是** | 章节 / 正文 / 角色 / 设定 / 时间线 / 世界事件（含内容变换钩子） |
| `hooks` | 流程钩子 | 否 | 内容保存 / 节点变化 / 登场变化 / 拼书文本 |
| `ui` | 界面扩展 | 否 | 侧栏面板 / 工具栏按钮 / 右键菜单 / 节点外观 / 悬浮卡 / 浮动角色 |
| `export` | 导出格式 | 否 | 注册新的导出格式 |
| `stats` | 统计规则 | 否 | 注册新的统计规则 |
| `theme` | 主题包 | 否 | 注册新的配色主题包 |
| `ai` | AI 动作 | 否 | 注册新的 AI 辅助动作 |
| `ai:read` | AI 用量 | 否 | 只读本机 AI 用量 / 花费与 DeepSeek 余额（不含 API Key） |
| `network` | 网络 | **是** | 插件联网（授予后才注入 `fetch`） |

- **没声明 = 没权限**：调用没声明的 API 会抛 `插件权限不足：需要 [data:read]，请在设置页授予`。
- 权限列表里出现未知 key 的，按**高风险**处理（必须显式授予）。
- 申请权限越少越好：只读数据的插件就别要 `data:write`，用户一眼就能看出风险。

---

## 六、界面槽位表

```
工具栏 toolbar     project.header    项目页顶部工具条
                  canvas.toolbar    流程图画布工具条
                  editor.toolbar    正文编辑器工具条

右键菜单 menu     canvas.node       画布右键 · 节点
                  canvas.blank      画布右键 · 空白处
                  canvas.link       画布右键 · 连线
                  editor.text       正文编辑器右键菜单

悬浮卡 hoverCard  node              流程图悬停迷你卡
                  role              角色悬浮卡

浮动角色 floatWidget
                  window.bottomRight / window.bottomLeft / window.topRight / window.topLeft
```

槽位错写成别的字符串会在加载时报错并列出可用值，不会「挂上去但永远不显示」。

---

## 七、返回值与 HTML 规则

回调的返回值决定宿主怎么展示：

```js
return '<div>…</div>'          // 以 '<' 开头 = HTML，进主题化弹窗 / 气泡 / 悬浮卡
return { html: '<div>…</div>' } // 同上（显式写法）
return '已完成 3 项'              // 普通字符串 = 界面底部提示条
return { text: '一句话' }        // 纯文本；浮动角色的「文字覆盖」用的就是这个
return undefined                // 什么都不显示
```

**HTML 会被清洗**（`src/renderer/src/plugins/sanitize.ts`）：`<script>`、`<style>`、所有 `on*` 内联事件、`javascript:` 一律剥掉。

所以：

- ❌ 不要写 `<script>`、`<style>`、`style="color:red"` 以外的内联脚本、`onclick=`
- ❌ 不要指望用 `<style>` 给插件内容排版
- ✅ 用**内联 `style="…"`**（属性本身允许）
- ✅ 直接用宿主提供的次要文字与标签 class：`.muted`（灰字）、`.chip-tiny` / `.chip-tiny.ok`（小标签）。外层容器（面板 `.plug-panel-pop` + `.plug-panel-body`、弹窗/气泡 `.plug-result-body`、悬浮卡 `.plug-hover-block`）由宿主自己包好，**插件 HTML 不用写这些外层 class**
- ✅ 用主题变量取色：`var(--text)`、`var(--text-dim)`、`var(--accent)`、`var(--surface)`、`var(--border)`、`var(--danger)`、`var(--warn)`…
- ✅ 一定要拼接用户数据时先转义：`esc(s) = String(s).replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;')`

---

## 八、浮动角色（`addFloatWidget`）

这是「常驻角色 / 桌面挂件」类插件的做法：一个角色图常驻主窗口一角，点一下弹气泡。

```js
api.ui.addFloatWidget(
  {
    id: 'pet',                    // 必填，同插件内唯一
    title: '我的角色',             // 气泡标题 / 无障碍名（默认取 id）
    slot: 'window.bottomRight',   // 四角之一，默认 window.bottomRight
    icon: '🐳',                   // 图读不到时的兜底表情（取前 2 个字符）
    image: 'pet.png',             // 插件目录内的相对路径（见下）
    width: 148,                   // 角色图宽（48–320，超出回落默认 120）
    hint: '点我看余额'             // 图上/下的短提示（最多 40 字）
  },
  (ctx) => {
    if (ctx && ctx.quipOnly === true) return { text: '一句有趣的话' }  // 见下面「点气泡」
    return { html: '<p>…</p>' }
  }
)
```

### 角色图

- `image` 是**插件目录内的相对路径**（如 `assets/pet.png`）：沙箱里读不到文件系统，主进程会替你把图读成 data URL 再交给界面。
- 支持 **png / jpg / jpeg / webp / gif / bmp**，单张 **≤ 8MB**；**不支持 svg**（避免脚本注入面）。
- 读不到时 `imageError` 有值，界面退化成 `icon` 或名字首字，插件仍然可用。
- 读取失败的原因会写进日志：`[plugin:名字] 浮动角色图读取失败：…`。

### 宿主免费给你的交互

注册之后不用自己实现这些：

- **点一下** → 调用你的回调，把返回的 HTML 显示成角色旁边的气泡；**2 秒后自动消失**。
- **再点气泡** → 宿主用 `{ quipOnly: true }` **再调用一次你的回调**，只取纯文本，铺成盖在气泡上的「文字覆盖」；再点一下关掉整个气泡。你可以用这个套路做「点一下冒一句有趣的话」（官方插件「造化笔冰冰」就是这么做的）。
- **按住** → 角色 Q 弹压扁，松手回弹，带一声鸭叫（音效可在右键设置里关）。
- **拖动** → 位置、大小（右键设置里的滑块）、收起状态都记在本机（localStorage，键前缀 `zaohuabi.plugFloat.<插件名>.<id>`），下次启动还在这。
- **右键** → 宿主的设置面板：大小 / 音效 / 显示（收起回到默认位置）。插件不用做这一层。
- **失焦渐隐** → 窗口失去焦点时角色淡出，点回来淡入。

### 气泡里能放什么

跟第七节一致：HTML 会被清洗，用内联样式 + 主题变量 + 现成 class。气泡里放表格、多行账单、进度条都可以，但**别放进整个页面**——它只有几百像素宽。

---

## 九、调试与排错

1. 改完代码 → 插件页「重新扫描」（或停用再启用）。
2. 插件卡上出现**加载错误**时，红字就是 `onLoad` 抛出的原始信息。
3. 日志前缀 `[plugin:你的插件名]`；统计规则 / 面板的报错会直接显示在对应卡片里，不会静默。
4. 用 `console.log(JSON.stringify(api.data.structure(bookId), null, 2))` 摸清数据形状再写逻辑。

**常见错误对照**

| 报错 | 原因 / 对策 |
|---|---|
| `插件不能 require 内置模块（受限沙箱）：fs` | 沙箱没有 Node 内置模块；用 `api.data` 取数据。 |
| `插件只能读取自己目录内的文件` | `require('./x.json')` 跑出了插件目录。 |
| `本地 JS require 暂不支持（基础版）` | 只能 require `.json`；把多个 JS 合并进 `index.js`。 |
| `插件权限不足：需要 [data:read]，请在设置页授予` | `manifest.permissions` 没声明，或高风险权限没授予。 |
| `不支持的槽位：xxx（可用：…）` | 槽位名写错，对照第六节表格。 |
| `界面动作 id 重复：xxx` | 同一插件里工具栏 / 菜单 / 悬浮卡 / 浮动角色 id 重名。 |
| `统计规则 id 重复：xxx` / `主题 id 重复：xxx` / `AI 动作 id 重复：xxx` | 同类注册 id 重复。 |
| `主题包缺少必需颜色令牌 --bg` | `--bg` / `--surface` / `--text` / `--accent` 四个必须都给。 |
| `角色图只能放在插件目录内：…` | `image` 用了相对路径却跳出了插件目录。 |
| `角色图不存在：…` / `不支持的图片格式：.svg` / `角色图过大（…KB，上限 8192KB）` | 图片路径、格式或体积不符合要求。 |
| `AI 用量服务未启用（该能力由主程序注入）` | 宿主没注入 AI 桥（一般只在本机开发模式下出现）。 |
| 回调返回 `[object Promise]` | 回调写成 `async` 了；宿主不 await，改成同步返回。 |

---

## 十、发布与分发

**给别人用（第三方插件）**：把插件文件夹打包发出去即可，对方解压到插件目录、点「重新扫描」、启用。

**做成随造化笔发布的官方插件**：

1. 放进仓库 `plugins-official/<目录名>/`。
2. `package.json` 的 electron-builder 配置要把 `plugins-official/**` 打进包里（`electron-builder.yml` 的 `files`），否则安装包里看不到。
3. 启动时会自动装进用户插件目录（`src/main/index.ts:329 provisionOfficialPlugins`）：
   - 用户没有 → 装上并**自动启用**；
   - 用户有、但官方包 `manifest.version` **更高** → 整目录替换（**所以每次改动都要把 version 提上去，否则用户拿不到新版本**）；
   - 用户版本不低于官方包 → 一个字节都不动（尊重用户改过 / 停用过的情况）。
4. 插件介绍（`description`）保持一句话概括，更新日志写进仓库的进度文档，不要塞进插件卡。

官方插件「造化笔冰冰」（`plugins-official/zaohuabi-bingbing/`）是一个可直接参考的完整例子：右下角浮动角色 + 点一下出三行账单 + 点气泡冒一句随机话（`quipOnly`）+ `api.ai` 只读用量，权限只有 `ai:read` / `ui`。

---

## 十一、照着抄：四个示例插件

| 示例（`plugins-samples/`） | 演示什么 | 权限 |
|---|---|---|
| `界面槽位演示` | 3 个工具栏槽位 + 3 个右键菜单槽位 + 节点外观 + 两类悬浮卡 | `data:read`, `ui` |
| `字数统计面板` | 侧栏面板 + 钩子订阅 + 自定义导出格式 | `data:read`, `ui`, `hooks`, `export` |
| `功能扩展演示` | 导出格式（自带 `.md` 扩展名）+ 统计规则 + 两套主题包 + 两个 AI 动作 | `data:read`, `export`, `stats`, `theme`, `ai` |
| `自动润色标记` | 正文保存时的内容变换（**会改稿**，需 `data:write`） | `data:read`, `data:write`, `hooks` |

### 最小浮动角色插件（完整代码，复制即用）

`manifest.json`

```json
{
  "name": "点头鲸",
  "version": "1.0.0",
  "description": "右下角一只鲸鱼，点一下说一句今天写了多少字。",
  "permissions": ["data:read", "ui"],
  "entry": "index.js"
}
```

`index.js`

```js
function esc(s) {
  return String(s == null ? '' : s).replace(/&/g, '&amp;').replace(/</g, '&lt;').replace(/>/g, '&gt;')
}

module.exports = {
  onLoad(api) {
    api.ui.addFloatWidget(
      { id: 'whale', slot: 'window.bottomRight', title: '点头鲸', icon: '🐳', image: 'whale.png', width: 128, hint: '点我看今天写了多少' },
      function (ctx) {
        if (ctx && ctx.quipOnly === true) return { text: '今天也在努力写呢' }
        const books = api.data.listBooks() || []
        if (books.length === 0) return { html: '<p class="muted">书架还空着</p>' }
        const rows = books.slice(0, 5).map(function (b) {
          const st = api.data.stats(b.meta.id)
          return '<li>' + esc(b.meta.name) + ' — 今日 ' + st.today + ' 字</li>'
        }).join('')
        return { html: '<p><b>今天写了多少</b></p><ul>' + rows + '</ul>' }
      }
    )
  }
}
```

> 这张图（`whale.png`）要自己放：没有它插件照常工作，只是角色退化成 icon 🐳。

---

## 附：一页速查

```
目录      <userData>/plugins/<文件夹>/{manifest.json, index.js, 图片…}
manifest  { name, version, description, author?, permissions[], entry? }
入口      module.exports = { onLoad(api) { … } }
权限      data:read data:write* hooks ui export stats theme ai ai:read network*
          （* = 高风险，需用户手动授予）
槽位      toolbar: project.header|canvas.toolbar|editor.toolbar
          menu:    canvas.node|canvas.blank|canvas.link|editor.text
          hoverCard: node|role      floatWidget: window.{bottomRight,bottomLeft,topRight,topLeft}
回调      必须同步；返回 '<…>' 或 { html } = 弹窗/气泡，字符串 = 提示条，{ text } = 纯文本
HTML      会被剥离 <script> <style> on* javascript:；用内联 style + 主题变量 + 现成 class
日志      [plugin:插件名] 前缀；插件卡上显示加载错误；改完点「重新扫描」
```
